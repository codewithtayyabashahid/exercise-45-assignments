//exercise 31
var users = ['sana', 'laiba', 'saba', 'Tayyaba', 'admin'];
if (users.length === 0) {
    console.log("we need to find users! ");
}
//remove all the username from your array
//and make sure correct message is printed
else {
    users = [];
    console.log("All users have been removed" + users.length);
}
